{
  "name": "shotwatcher",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://issnoe.github.io/shotwatcher/",
  "dependencies": {
    "@material-ui/core": "^3.9.0",
    "@material-ui/icons": "^3.0.2",
    "babel-cli": "^6.26.0",
    "babel-preset-flow": "^6.23.0",
    "gh-pages": "^1.0.1",
    "react": "^16.7.0",
    "react-dom": "^16.7.0",
    "react-scripts": "2.1.3",
    "react-webcam": "^1.1.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "build:babel": "babel src/ -d lib/",
    "prepublish": "yarn run build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "deploy:manual": "node scripts/deploy.js"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "flow-bin": "^0.91.0"
  }
}
